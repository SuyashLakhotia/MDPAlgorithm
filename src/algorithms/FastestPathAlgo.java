package algorithms;

import map.Map;
import robot.Robot;

/**
 * Fastest path algorithm for the robot.
 *
 * @author Suyash Lakhotia
 */

public class FastestPathAlgo {
    private Map map;
    private Robot bot;

    public FastestPathAlgo(Map map, Robot bot) {
        this.map = map;
        this.bot = bot;
    }

    public StringBuilder runFastestPath(Map map, int goalRow, int goalCol) {
        return new StringBuilder();
    }
}
